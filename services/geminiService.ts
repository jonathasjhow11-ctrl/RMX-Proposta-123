
// Serviço de IA desativado para manter o app 100% local e offline.
export const geminiService = {
  optimizeDescription: async (description: string): Promise<string> => {
    return description;
  }
};
